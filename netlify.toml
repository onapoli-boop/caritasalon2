[build]
  command = "npm run build:web"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[context.production]
  environment = { EXPO_PUBLIC_SUPABASE_URL = "$EXPO_PUBLIC_SUPABASE_URL", EXPO_PUBLIC_SUPABASE_ANON_KEY = "$EXPO_PUBLIC_SUPABASE_ANON_KEY" }
